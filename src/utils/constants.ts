/**
 * 常量定义
 * 所有可复用或可配置的值集中管理
 */

// 扩展名称
export const EXTENSION_NAME = 'My Test Extension';

// Storage Keys
export const STORAGE_KEY = {
  SETTINGS: 'settings',
  USER_DATA: 'userData',
} as const;

// 其他常量
export const DEFAULT_TIMEOUT = 5000;
